# PyLab
Useful Python examples, e.g. plots
Please make sure you don't have the PyLab package installed, which seemingly is now deappreciated
p21c_tools.py and PyLab.py contains some useful functions, to use them from any locations, add this to your bash_profile:
export PYTHONPATH=Current_Path:${PYTHONPATH}
